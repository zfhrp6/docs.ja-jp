---
title: コンパイラの警告 (レベル 3) CS1718
ms.date: 07/20/2015
f1_keywords:
- CS1718
helpviewer_keywords:
- CS1718
ms.assetid: 7c1c11fd-4f91-482d-b8f7-efe2a361634e
ms.openlocfilehash: 303aec0ae3d680b09f9c73aa67b9a5853379c1a9
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 05/04/2018
---
# <a name="compiler-warning-level-3-cs1718"></a><span data-ttu-id="822a6-102">コンパイラの警告 (レベル 3) CS1718</span><span class="sxs-lookup"><span data-stu-id="822a6-102">Compiler Warning (level 3) CS1718</span></span>
<span data-ttu-id="822a6-103">同じ変数と比較されました。他の変数と比較しますか?</span><span class="sxs-lookup"><span data-stu-id="822a6-103">Comparison made to same variable; did you mean to compare something else?</span></span>  
  
 <span data-ttu-id="822a6-104">別のものと比較する場合は、単にステートメントを修正する必要があります。</span><span class="sxs-lookup"><span data-stu-id="822a6-104">If you meant to compare to something else, then you should simply correct the statement.</span></span>  
  
 <span data-ttu-id="822a6-105">あるいは、 `if (a == a) (true)` や `if (a < a) (false)`などのステートメントによって真または偽のテストを実施しようとした可能性もあります。</span><span class="sxs-lookup"><span data-stu-id="822a6-105">But another possibility is that you were testing for true or false, and were doing so by statements such as `if (a == a) (true)` or `if (a < a) (false)`.</span></span> <span data-ttu-id="822a6-106">単に `if (true)` や `if (false)`とする方が適切です。</span><span class="sxs-lookup"><span data-stu-id="822a6-106">It is better to simply say `if (true)` or `if (false)`.</span></span> <span data-ttu-id="822a6-107">これには、次の 2 つの理由があります。</span><span class="sxs-lookup"><span data-stu-id="822a6-107">There are two reasons for this:</span></span>  
  
-   <span data-ttu-id="822a6-108">こちらの方が簡単です。率直な表現の方が常に明快になります。</span><span class="sxs-lookup"><span data-stu-id="822a6-108">It is simpler: it is always clearer to simply say what you mean.</span></span>  
  
-   <span data-ttu-id="822a6-109">混乱の回避に役立ちます。C# 2.0 の新機能は Null 許容の値型ですが、これは T-SQL (SQL Server で使用するプログラミング言語) の値 `null` に似ています。</span><span class="sxs-lookup"><span data-stu-id="822a6-109">It helps avoid confusion: a new feature of C# 2.0 is nullable value types, which are analogous to the value `null` in T-SQL, the programming language used by SQL Server.</span></span> <span data-ttu-id="822a6-110">T-SQL では三値論理を使用するため、T-SQL に慣れた開発者は、Null 許容型が `if (a == a)`などの式に及ぼす影響について懸念する可能性があります。</span><span class="sxs-lookup"><span data-stu-id="822a6-110">Developers familiar with T-SQL might be concerned about the effect of nullable types on expressions such as `if (a == a)`, because of the use of ternary logic in T-SQL.</span></span> <span data-ttu-id="822a6-111">`true` または `false`を使用すると、このような混乱を避けることができます。</span><span class="sxs-lookup"><span data-stu-id="822a6-111">If you use `true` or `false`, you avoid this possible confusion.</span></span>  
  
## <a name="example"></a><span data-ttu-id="822a6-112">例</span><span class="sxs-lookup"><span data-stu-id="822a6-112">Example</span></span>  
 <span data-ttu-id="822a6-113">次のコードでは、警告 CS1718 が生成されます。</span><span class="sxs-lookup"><span data-stu-id="822a6-113">The following code generates warning CS1718.</span></span>  
  
```csharp  
// CS1718.cs  
using System;  
public class Tester   
{  
    public static void Main()   
    {   
        int i = 0;  
        if (i == i) { // CS1718.cs  
        //if (true) {   
            i++;  
        }  
    }  
}  
```
